<%= form_for @episode do |f| %>
  <%= select_tag "Show for A Plot", options_for_select(@show_names.uniq, "") %>

  <%= select_tag "Show for B Plot", options_for_select(@show_names.uniq, "") %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.submit class: "btn btn-info" %>
<% end %>
